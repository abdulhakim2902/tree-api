export const enum RelationType {
  BLOOD = 'blood',
}

export const enum SiblingRelationType {
  HALF = 'half',
}

export const enum SpouseRelationType {
  DIVORCED = 'divorced',
  MARRIED = 'married',
}

export const enum ChildrenRelationType {
  ADOPTED = 'adopted',
}

export const enum ParentRelationType {
  ADOPTIVE = 'adoptive',
}
